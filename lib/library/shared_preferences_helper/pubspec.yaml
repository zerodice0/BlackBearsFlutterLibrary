name: shared_preferences_helper
description: this package is made for using easily shared_preferences pacakge. if you make SharedPreferencesHelper object with key, you don't need to care about key when fetching, updating, removing. :).
version: 0.0.1
homepage: https://github.com/zerodice0/BlackBearsFlutterLibrary

environment:
  sdk: ">=2.13.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
